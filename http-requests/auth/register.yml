info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Vitor",
        "email": "vitor.lima@ifms.edu.br",
        "password": "P@$$w0rd"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
